syntax = "proto3";
package tbot;

import "trade_event.proto";
import "metric_event.proto";

message ObserverMessage {
  string schema_version = 1;
  oneof payload {
    TradeEvent event = 2;
    MetricEvent metric = 3;
  }
}
