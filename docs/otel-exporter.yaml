exporters:
  otlp:
    endpoint: http://localhost:4318
    protocol: http/protobuf
    compression: gzip

service:
  pipelines:
    traces:
      exporters: [otlp]
    metrics:
      exporters: [otlp]
    logs:
      exporters: [otlp]
